name: CI
on:
  push:
    branches: [master]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run:
          curl -X POST
          https://api.cloudflare.com/client/v4/pages/webhooks/deploy_hooks/c980cb2e-ca5f-41bb-bd18-61b71f2d489a
